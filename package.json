{
	"scripts": {
		"install-deps": "cd client && npm ci && cd ../server && npm ci",
		"dev-client": "cd client && npm run dev",
		"dev-server": "cd server && npm run dev",
		"build-prod": "cd client && npm run build"
	}
}
